import { Text, View } from 'react-native'
import { ScrollView } from 'react-native/types_generated/index';

export default function ListaAlunos() {
    const alunos =["Julia", "Paty", "Bruno", "Fernanda"];
  return (
<View>
    
    <ScrollView>
    <Prime />
    <Prime/>
    </ScrollView>

</View>
  )
}
